import { PlayerTagInput } from "components/PlayerTagInput.slint";
import { EZStreamWindowPreferences as WP } from "WindowPreferences.slint";

export component MainWindow inherits Window {
    title: "EZ-Stream";
    width: 600px;
    height: 300px;

    background: WP.color-scheme.background-color;

    VerticalLayout {
        spacing: 10px;
        alignment: center;

        HorizontalLayout {

            alignment: LayoutAlignment.space-evenly;

            PlayerTagInput {
                placeholder-text: "Player 1 Tag";
                font-size: WP.font-size;
                width: 200px;
                height: 20px;
            }

            PlayerTagInput {
                placeholder-text: "Player 2 Tag";
                font-size: WP.font-size;
                width: 200px;
                height: 20px;
            }
        }

        HorizontalLayout {
            alignment: center;

            PlayerTagInput {
                placeholder-text: "Test";
                font-size: WP.font-size;
                width: 200px;
                height: 20px;
            }
        }
    }
}
